<form method="post" action="{{ path('app_client_profile_delete', {'id': client_profile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client_profile.id) }}">
    <button class="btn">Delete</button>
</form>
